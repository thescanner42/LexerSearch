patterns:
- |-
  free($_VAR);
  ..}
  $_VAR = NULL;
  ..}
  free($_VAR);
- |-
  free($_VAR);
  ..}
  $_VAR = 0;
  ..}
  free($_VAR);
- |-
  free($_VAR);
  ..}
  $_VAR = malloc(...);
  ..}
  free($_VAR);
name: exclude
group:
  name: double_free
  # match: # no need to state in non-anchor pattern
  #   - _VAR
  cancel: true
languages:
  - c
