patterns: # "anchor" pattern - has small span intersection
  - TransportGenerator.create(...)
  - $_VAR = TransportGenerator.create(...)
languages:
  - java
group:
  name: travel_group
  match:
    - _VAR
name: anchor