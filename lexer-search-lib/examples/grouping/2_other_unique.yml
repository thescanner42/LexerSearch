patterns: # color from anchor
  - >
    $_VAR = TransportGenerator.create(...)
    ..}
    $_VAR.color($COLOR)
languages:
  - java
transform:
  COLOR: ^[a-zA-Z_]
group:
  name: travel_group
  unique: true
  match:
    - _VAR
name: color