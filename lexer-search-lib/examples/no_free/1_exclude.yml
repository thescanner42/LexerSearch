patterns:
- |-
  $_VAR = malloc(...);
  ..}
  free($_VAR);
name: exclude
group:
  name: malloc_no_free
  # match: # no need to state in non-anchor pattern
  #   - _VAR
  cancel: true
languages:
  - c
transform: {}
